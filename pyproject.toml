[project]
name = "speakforwater-api"
version = "1.0.0"
description = "Automated podcast generation from water resources research papers via NotebookLM"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
